import { darkTheme, pink, neutral } from '@superys/momo-ui';

const customTheme = {
  ...darkTheme,
  primary: { main: pink[400], hover: pink[300], contrast: neutral[100] },
};

export default customTheme;
